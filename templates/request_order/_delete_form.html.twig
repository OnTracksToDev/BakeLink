<form method="post" action="{{ path('app_request_order_delete', {'id': request_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ request_order.id) }}">
    <button class="btn">Delete</button>
</form>
