<form method="post" action="{{ path('app_comment_pastry_chef_delete', {'id': comment_pastry_chef.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment_pastry_chef.id) }}">
    <button class="btn">Delete</button>
</form>
