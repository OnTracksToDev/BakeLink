<form method="post" action="{{ path('app_pastry_chef_delete', {'id': pastry_chef.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pastry_chef.id) }}">
    <button class="btn">Delete</button>
</form>
